Approach:
swap first and last node 
put root node to its correct position (heapify)

void heapSort( int arr[], int n){
 int size= n;
 while(size > 1){
  swap(arr[size],arr[1]);
  size--;
  
  heapify(arr, size, 1);
 }
}

int main(){
 for(int i= n/2; i > 0; i--){
  heapify(arr, n, i)
 }
 heapSort(arr,n);
 return 0;  
}
